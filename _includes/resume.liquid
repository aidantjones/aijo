<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
  </head>
  <body>
    {% include header.liquid %}
    <h1>{{ title }}</h1>
    {{ content }}
  </body>
</html>
